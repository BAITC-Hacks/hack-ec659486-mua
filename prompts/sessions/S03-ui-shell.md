# S03 — Каркас интерфейса (волна 1, 14:15–14:40, B, ветка `s/ui-shell`)

Прочитай `AGENTS.md`, `docs/case.md`, `docs/spec-case11.md` §1, §6; `frontend/lib/types.ts`, если уже есть в `origin/s/contract`.

**Пишешь только в:** `frontend/app/layout.tsx`, `frontend/app/page.tsx` (заглушка), `frontend/app/runs/[id]/page.tsx` (заглушка), `frontend/app/report/[id]/page.tsx` (заглушка с четырьмя вкладками без данных), `frontend/components/status-badge.tsx`, `frontend/components/ui/tabs.tsx`.
**Не трогаешь:** `lib/`, остальные компоненты.

Задача: «розетки» фронта: навигация и все страницы существуют и открываются, чтобы S06/S07/S12 писали каждый в свой файл и не трогали `layout.tsx`.

Сделай:
1. `layout.tsx`: название «ОргДифф — анализ оргструктуры и функционала», подзаголовок «выводы рекомендательные, каждый со ссылкой на пункт документа», навигация: Загрузка (`/`), Отчёт (`/report/demo`); индикатор режима LLM из `/health` (уже есть `components/health-badge.tsx`).
2. Заглушки страниц с заголовками и пустыми состояниями на русском; `report/[id]`: вкладки «Подразделения», «Функции», «Дубли и конфликты», «Заключение» (компонент `ui/tabs.tsx` без внешних библиотек).
3. `status-badge.tsx`: цвета статусов kept/changed/lost/new/moved/created/abolished/transformed с русскими подписями; плюс нейтральные метки `verification` (exact — «дословно», lexical — «по тексту», llm — «проверено моделью») и предупреждающая метка «требует проверки» для `verified: false` (пропс `verified?: boolean`).

Никаких запросов к API и фикстур — это S06/S07/S12.

**Done-when:** `cd frontend && npm run build` зелёный, каждый маршрут из навигации открывается.
**Cut-if:** вкладки как простые ссылки-якоря.

В конце выведи список изменённых файлов и вывод Done-when.

---

## Режим работы: автономно, до зелёного Done-when

Ты работаешь в GUI, человек к тебе не вернётся до отчёта. Рабочая папка — worktree ветки `s/ui-shell`.
Порядок: реализуй → **сам выполни команду Done-when** → красная: почини и повтори (до трёх попыток) →
зелёная: `git add -A && git commit -m "feat(S03): <что сделано>" && git push -u origin s/ui-shell`.
Затем создай `status/agents/S03.md` в этом worktree: первая строка `# S03 — ЗЕЛЕНО` или `# S03 — КРАСНО`,
далее команда Done-when и её вывод, список изменённых файлов, что не сделано и почему; закоммить и запушь его так же.
Запреты: не пушить и не мержить в `main`; не трогать файлы вне своей строки матрицы владения; не добавлять зависимости;
не переписывать README и чужие тесты. После трёх красных попыток — отчёт «КРАСНО» с выводом ошибки и остановись.
