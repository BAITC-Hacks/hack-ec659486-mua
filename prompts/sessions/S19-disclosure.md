# S19 — Журнал AI и раскрытие (волна 5, 16:40–17:15, A, ветка `s/disclosure`)

Прочитай `AGENTS.md`, `docs/case.md` (обязательные условия задачи — приоритет над всем),
`docs/spec-case11.md` §8 (раскрытие), `backend/app/prebuilt/NOTICE.md`, `docs/disclosure-ready-text.md`
и свою строку в матрице владения `docs/sessions.md`. Факты по сессиям — `status/agents/S*.md` и `git log --merges main`.

**Пишешь только в:** `docs/ai-usage.md`.
**Не трогаешь:** README (это S18), код.

Задача: след использования инструментов, который выдержит проверку. По 5.4.7 эксперты
вправе проверять историю разработки и фактический вклад каждого участника.

Сделай:
1. Строка на каждую сессию дня (S01–S20, включая ручную S15 «живой прогон с ключом и сохранение фикстур»): номер и название, инструмент, идентификатор сессии,
   автор A или B, ветка, SHA мержа, затронутые файлы, чем проверено (команда Done-when).
2. Инструменты перечисляй честно и полностью: Codex, Claude Code, Antigravity — что чем
   делалось. По 5.4.12 разрешены любые AI-инструменты, скрывать нечего.
3. **Не приписывай трейлер Codex работе, которую он не делал.**
4. Отдельный раздел «Подготовлено до старта» — то же, что в README §12: каркас FastAPI + Next.js + compose (22.09),
   `backend/app/prebuilt/lib_docx.py` и `lib_normalize.py` написанные командой ранее (внутренние утилиты) (ссылка и SHA из `NOTICE.md`),
   промпты сессий и документы подготовки; что из этого — домен кейса 11 (ничего: логика P2–P6, промпты, схемы и интерфейс написаны 23.09).
5. Две-три чистые иллюстрации: окно сессии без ключей, токенов и личных данных в кадре.
6. Отдельным абзацем — что писалось руками и что переписывалось после генерации; отдельно — mock-фикстуры:
   какие записаны с реального ответа модели (S15), какие составлены вручную.

**Done-when:** каждая ветка, принятая мерж-поездом, встречается в журнале ровно один раз,
и по строке можно найти коммит: `git log --merges --oneline main` против таблицы.
**Cut-if:** без иллюстраций — таблица сессий и раздел «Подготовлено до старта».

В конце выведи список изменённых файлов и вывод команды Done-when.
В `main` не пушь и не мержи — это делает B через `scripts/ops/merge-train.sh`.
