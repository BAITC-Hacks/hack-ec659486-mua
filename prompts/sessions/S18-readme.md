# S18 — README (волна 5, 16:40–17:15, B, ветка `s/readme`)

Прочитай `AGENTS.md`, `docs/case.md` (обязательные условия задачи — приоритет над всем),
`docs/spec-case11.md` (§1 демо-путь, §2 пайплайн, §4 API, §8 раскрытие), `docs/launch.md` и свою строку в матрице владения `docs/sessions.md`.
Дополнительно: `docs/disclosure-ready-text.md`, `backend/app/prebuilt/NOTICE.md`, `docs/regulations-2026-09-22.md` §2.2–2.4, отчёты `status/agents/S*.md` (что реально влито).

**Пишешь только в:** `README.md`. **Не трогаешь:** код. Нашёл расхождение — сообщи владельцу файла.

README — условие допуска (5.4.16, 5.6.5): проект, который не запускается по инструкциям из репозитория,
к отбору не допускается, исправления после дедлайна не принимаются. Критерий «README и воспроизводимость» — 25 баллов из 100.
AI-судья, скорее всего, проверяет README по структуре официального промпта организаторов — поэтому она первична.

## Шаг 1. Официальный промпт организаторов — выполни дословно, структура и нумерация разделов 1–11 ровно такие

Проанализируй текущий проект и создай для него полноценный README.md на русском языке.
README должен быть понятен жюри хакатона и содержать:
1. Название проекта
2. Краткое описание — какую проблему решает проект и для кого.
3. Что реализовано — основные функции и возможности решения.
4. Как работает решение — кратко опиши основной пользовательский сценарий от входных данных до результата.
5. Технологии — языки, фреймворки, библиотеки, AI-модели, API и внешние сервисы.
6. Архитектура проекта — основные компоненты и как они взаимодействуют.
7. Установка и запуск — пошаговая инструкция с необходимыми командами.
8. Как проверить решение — пример сценария, который может повторить жюри.
9. Данные и интеграции — какие источники данных, API или внешние сервисы используются.
10. Ограничения — что не реализовано или какие ограничения есть у текущей версии.
11. Ссылка на deployed-версию, если она существует.
Используй только информацию, которую можно подтвердить по текущему репозиторию. Не придумывай функции,
технологии или результаты, которых в проекте нет. Оформи README аккуратно в Markdown.

## Шаг 2. Дополнения внутри тех же разделов (требования Положения, которых нет в промпте)

- Раздел 3 «Что реализовано» — по пяти обязательным условиям из `docs/case.md`, ровно в их порядке: реорганизованные / сохранённые / созданные подразделения; сопоставление функций и потеря функций; дублирование и конфликт интересов; источник (документ + пункт) у каждого вывода; заключение. Что из этого работает в Cut-if-варианте (например, `transformed` не находим) — так и написать.
- Раздел 4 «Как работает» — шесть шагов P1–P6 из `docs/spec-case11.md` §2 одной таблицей: что делает код, а что LLM. Сказать явно: разбор по пунктам и сопоставление одинаковых текстов — детерминированный код; LLM только на языковых шагах, через Structured Outputs (strict JSON-схемы); номера пунктов не из документа отбрасываются кодом.
- Разделы 7 и 8 бери из `docs/launch.md` как основу и подставляй факты со сдаваемого SHA. В README **только** `git`, `docker compose`, `uv`, `npm`, `python`; там, где синтаксис отличается, отдельная строка для Windows PowerShell. **Никаких `make`, `bash`, `sh`, `scripts/*.sh`, `deploy/*.sh`** — у эксперта на Windows их нет, а по 5.4.16 «не запустилось по инструкции» = недопуск.
- В разделе 7 «Установка и запуск» обязательно: **зависимости с версиями** (Docker, Compose, при запуске без Docker — Python, Node), **параметры окружения** (таблица переменных из `.env.example` с назначением и значением по умолчанию: `LLM_MODE`, `OPENAI_API_KEY`, `OPENAI_MODEL`, порты), порты, память, измеренное время первой сборки. Это пункты 5.4.15, без них README неполный.
- В разделе 8 «Как проверить» — пронумерованные шаги, которые ты **сам прошёл на чистом клоне без ключа OpenAI**: `docker compose up --build` (и запасной путь без Docker: `uv sync` + `uv run uvicorn ...`, `npm ci` + `npm run dev`) → открыть `http://localhost:3000` → кнопка «Тестовый комплект» (редакции 8 и 9 Положения о внутреннем аудите) → прогресс по шагам → отчёт: какие именно подразделения, утраченные функции, дубли и конфликты эксперт должен увидеть на вкладках (перечисли по факту из `mocks/demo_report.json` и живого прогона) → клик по «п. N» открывает текст пункта → «Скачать заключение (.md)». Эксперт проверяет без личных аккаунтов (5.6.6): первым идёт сценарий в `LLM_MODE=mock`, отдельным подпунктом — что меняется с ключом (`LLM_MODE=live`: свои .docx «до»/«после»), и что в mock-режиме на своих документах пользователь получает честное сообщение «нужен ключ OpenAI».
- В разделе 9 «Данные и интеграции» — тестовый комплект от организаторов (`data/case11/`, две редакции Положения, обезличено), OpenAI Responses API (модель — из `.env.example`), mock-фикстуры (`backend/app/mocks/`: реальные ответы модели, сохранённые при прогоне с ключом, — или составленные вручную, если S15 не успел; писать честно по статус-файлам). Числа — **только из `stats` фактического прогона** на тестовом комплекте; без слов «находит все изменения», «100 % точность», «заменяет эксперта».
- В разделе 10 «Ограничения» — честно: только .docx (PDF/Excel — вежливая ошибка), НПА и другие операторы (опции 1–2) не реализованы, выводы рекомендательные, что вырезано по Cut-if, что не проверялось на других документах.
- Добавь раздел **12. Заранее подготовленные компоненты и AI-инструменты** — блок из `docs/disclosure-ready-text.md` с подставленными ссылками и SHA (5.4.4, 5.4.4.2, 5.4.12): каркас FastAPI + Next.js + compose (22.09), `backend/app/prebuilt/lib_docx.py` и `lib_normalize.py` написанные командой ранее (внутренние утилиты) (см. `NOTICE.md`), документы тестового комплекта — от организаторов; промпты, схемы, логика P2–P6 и интерфейс — 23.09; какие модули сгенерированы Codex/Claude Code (по `docs/ai-usage.md`).
- Добавь раздел **13. Команда** — кто что делал, по файлам.

Плейсхолдеров `ORG/REPO`, `URL`, `<...>` в сданной версии быть не должно. Если deployed-версии нет — в разделе 11 так и написать.

**Done-when:** второй человек на своём ноутбуке проходит раздел 8 «Как проверить» с нуля, без ключа,
и получает описанный отчёт на тестовом комплекте, не задав ни одного вопроса.
**Cut-if:** сократи всё, кроме разделов 1–11 официального промпта и раздела 12.

В конце выведи список изменённых файлов и результат Done-when.
В `main` не пушь и не мержи — это делает B через `scripts/ops/merge-train.sh`.
