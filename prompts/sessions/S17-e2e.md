# S17 — Сквозной тест по «Как проверяют» (волна 4, 16:00–16:40, B, ветка `s/e2e`)

Прочитай `AGENTS.md`, `docs/case.md` (раздел «Как проверяют» — это сценарий эксперта, его и повторяем), `docs/spec-case11.md` §1, §3, §4. Возьми актуальный `main` (`git fetch origin main && git merge origin/main`): к этому моменту влиты S08 (`app/pipeline.py`, `app/routers/runs.py`), S05, S09–S11, S14. Модели — `backend/app/schemas.py`, фикстуры — `backend/app/mocks/`. Образец теста с ожиданием статуса — `backend/tests/test_runs.py` (S08), его не править.

**Пишешь только в:** `backend/tests/test_e2e_mock.py`, `deploy/smoke.sh`.
**Не трогаешь:** `main.py`, `schemas.py`, `layout.tsx`, модули шагов, роутеры, фикстуры, чужие тесты, README. Новых зависимостей нет.

Задача: один тест, который делает ровно то, что сделает эксперт на контрольном комплекте: запускает тестовый комплект и проверяет, что агент нашёл реорганизацию подразделения, потерю функции и дублирование, и что у каждой находки настоящий источник. Плюс те же проверки через `curl` в smoke-скрипте.

Сделай:
1. `backend/tests/test_e2e_mock.py` (TestClient, `LLM_MODE=mock`, ключ не нужен): `POST /api/runs/demo` → `run_id`; polling `GET /api/runs/{run_id}` каждые 0,5 с до `done`, таймаут 90 с; статус `error` — падение с текстом `error` из ответа; `GET .../report` валидируется моделью `Report`.
2. Проверки содержания (каждая — отдельный `assert` с русским сообщением, чтобы в выводе было видно, какое условие ТЗ не выполнено): ≥ 1 `UnitChange` со статусом `transformed` или `created`; ≥ 1 `FunctionMatch` со статусом `lost`; ≥ 1 `Duplicate`; `conclusion_md` непустой и не содержит «не сформировано».
3. Прослеживаемость: у каждой находки (`unit_changes`, `function_matches` — через `before`/`after`, `duplicates` — через обе функции, `conflicts`) `sources` непустые; для **каждого** `Source` сделай `GET /api/runs/{run_id}/clauses/{doc_id}/{clause_number}` → 200, `number` совпадает, а первые 40 символов `quote` встречаются в `text` пункта (пробелы нормализуй). Выдуманный пункт = красный тест — это и есть цель.
4. Отдельный тест: `GET .../report.md` → `text/markdown`, тело содержит `conclusion_md`.
5. `deploy/smoke.sh`: замени блок `/api/example` на продуктовый сценарий: `curl -fsS -X POST $BACKEND/api/runs/demo` → извлечь `run_id` (`python3 -c` или `sed`, без `jq`); цикл ожидания `"status":"done"` до 120 с, `error` → `fail` с телом ответа; `GET .../report` с `-f`, проверить непустые `unit_changes`, `function_matches`, `duplicates` (через `python3 -c 'import json,sys; ...'` и `sys.exit(1)`); `GET .../report.md` с `-f`; `POST /api/runs` с файлом `.pdf` → HTTP 400. Блоки `/health`, «ошибки не раскрывают внутренности» и `frontend /` оставь.

Ничего не чинить в пайплайне: если условие не выполняется — тест красный, в `status/agents/S17.md` напиши, какое именно и что вернул API. Это сигнал интегратору, а не повод ослабить проверку.

**Done-when:** `cd backend && LLM_MODE=mock uv run pytest -q tests/test_e2e_mock.py`
**Cut-if:** проверка цитаты в п. 3 только по `number` (200 и совпадение номера); в smoke.sh — без проверки `.pdf`.

В конце выведи список изменённых файлов и вывод Done-when.


## Дополнение по аудиту: независимый эталон

- Ожидаемые находки берутся не «≥1 потеря», а из независимого детерминированного диффа `data/case11/diff-8-vs-9.md` (47 изменённых пар, 24 кандидата в потери, 23 новых): проверь как минимум — реорганизация: в ред. 9 появились ДИТААД и ДОА (пункты 3.4 а/б), «Директор направления внутреннего аудита» (ред. 8, 3.4.а) преобразован; потеря-кандидат: ред. 8 «3.6 организует контроль устранения недостатков и нарушений…» и «3.11/4.10 участвует в разработке проектов документации» должны быть либо сопоставлены с проверкой, либо помечены как потери/кандидаты с источником ред. 8; новое: ред. 9 «а. информирует о потенциальном конфликте при совмещении…».
- Тест проверяет, что статус `done` (не `partial`), каждый источник открывается через `clauses`, цитата — подстрока пункта, и число `unverified_candidates` присутствует в stats.

---

## Режим работы: автономно, до зелёного Done-when

Ты работаешь в GUI, человек к тебе не вернётся до отчёта. Рабочая папка — worktree ветки `s/e2e`.
Порядок: реализуй → **сам выполни команду Done-when** → красная: почини и повтори (до трёх попыток) →
зелёная: `git add -A && git commit -m "feat(S17): <что сделано>" && git push -u origin s/e2e`.
Затем создай `status/agents/S17.md` в этом worktree: первая строка `# S17 — ЗЕЛЕНО` или `# S17 — КРАСНО`,
далее команда Done-when и её вывод, список изменённых файлов, что не сделано и почему; закоммить и запушь его так же.
Запреты: не пушить и не мержить в `main`; не трогать файлы вне своей строки матрицы владения; не добавлять зависимости;
не переписывать README и чужие тесты. После трёх красных попыток — отчёт «КРАСНО» с выводом ошибки и остановись.
