# Таймбоксы 23.09 (кейс 11) — для людей и штурмана

Собрано из `prompts/sessions/S*.md` и листа запуска `prompts/sessions/README.md`. Контрольные точки: 15:20, 16:00, 16:40 — мерж-поезд и тег `green-HHMM`; 16:40 — ядро end-to-end в mock-режиме; 17:15 — freeze; 17:45 — сдача. S15 — ручная сессия A с ключом: живой прогон тестового комплекта и сохранение реальных ответов модели в `backend/app/mocks/`.

| Сессия | Волна | Кто | Ветка | Что | Done-when | Cut-if |
|---|---|---|---|---|---|---|
| S01 | 1 14:15–14:40 | A | `s/contract` | Контракт и «розетки» | `cd backend && uv run pytest -q` (smoke-тест проверяет и страницу /docs со всеми эндпоинтами spec §4). | `GET .../clauses/...` и `report.md` можно оставить заглушками с пустым телом. |
| S02 | 1 | B | `s/fixtures` | Фикстуры отчёта | `cd backend && uv run pytest -q tests/test_fixtures_valid.py` зелёный. | одна Duplicate и один UnitChange каждого типа. |
| S03 | 1 | B | `s/ui-shell` | Каркас интерфейса | `cd frontend && npm run build` зелёный, каждый маршрут из навигации открывается. | вкладки как простые ссылки-якоря. |
| S04 | 2 14:40–15:20 | A | `s/parse` | Разбор .docx в пункты | `cd backend && uv run pytest -q tests/test_parse.py` | `kind` всегда `other`; уровень пункта не вычислять; `lead_in` только по родительскому пункту с двоеточием (без поиска абзацев без номера) — но `modality` для пунктов под «не имеют права:» обязана быть `prohibition`, это не режется. |
| S05 | 2 | A | `s/units` | Подразделения: извлечение и сопоставление | `cd backend && LLM_MODE=mock uv run pytest -q tests/test_units.py`. | `match_units` только детерминированный (`transformed` не находим, честно пишем `abolished`/`created`), LLM-шаг остаётся в коде за флагом. |
| S06 | 2 | B | `s/ui-upload` | Страница загрузки | `cd frontend && npm run build` | drag-and-drop убрать, оставить только `<input type="file">`; список файлов — простыми именами без кнопки «убрать». |
| S07 | 2 | B | `s/ui-report` | Страница отчёта на фикстуре | `cd frontend && npm run build` | фильтр в матрице функций; markdown как `<pre>`; панель источника — `<details>` под таблицей. |
| S08 | 2 | A | `s/runs` | Оркестратор и API запусков | `cd backend && LLM_MODE=mock uv run pytest -q tests/test_runs.py` | `clauses/...` отдаёт 404 с текстом «панель источника — S12»; multipart-загрузка валидирует только расширение. |
| S09 | 3 15:20–16:00 | A | `s/functions` | Извлечение функций по пунктам | `cd backend && LLM_MODE=mock uv run pytest -q tests/test_functions.py` | категория только task|function|duty; `context_clause_numbers` заполняются только кодом из `section_path`; привязка к подразделению не делается — все функции документа с `unit_id=""`, честно отмечено в `note` статуса. Разделение на `functions`/`constraints` по модальности и `executor` из `lead_in` не режутся. |
| S10 | 3 | A | `s/matching` | Кандидаты и проверка сопоставления функций до↔после | `cd backend && LLM_MODE=mock uv run pytest -q tests/test_matching.py` | dense (шаг 2) — только чтение кэша, без вызова embeddings; `merge`/`partial` не различаются (всё как `changed` с `kind = one_to_one`); `confirm_loss` без второго поиска по сырым пунктам, только по функциям «после». Ни при каком Cut-if похожесть не становится статусом и `lost` без `confirm_loss` не получает `verified = True`. |
| S11 | 3 | A | `s/conflicts` | Дубли и конфликты интересов | `cd backend && LLM_MODE=mock uv run pytest -q tests/test_conflicts.py` | LLM `verify_duplicates` не вызывается — дубли возвращаются только как `verified=False` «требует проверки»; правила только (а) и (в); `explain_conflict` не вызывается — `verified=False`, `explanation` собирается кодом из `title` и цитат с `verification_note` «требует проверки: объяснение LLM не выполнено». |
| S12 | 3 | B | `s/ui-wire` | Фронт на живом API | `cd frontend && npm run build` | закрытие по Escape; пометка про фикстуру; полоса прогресса — только текст «N %». |
| S13 | 3 | B | `s/schemas` | Схемы LLM-вызовов | `cd backend && uv run pytest -q tests/test_llm_schemas.py` зелёный. | тест (г) только для `extract_functions` и `verify_matches`; `validate_clause_numbers` — только для `extract_units`, `extract_functions`, `verify_matches`; тест (д) и `model_validator` дублей — убрать. |
| S14 | 4 16:00–16:40 | A | `s/conclusion` | Заключение и экспорт | `cd backend && LLM_MODE=mock uv run pytest -q tests/test_conclusion.py` | рекомендации — пустой список с честной строкой «рекомендации не сформированы»; в экспорте таблица функций только для статусов `lost/new/moved`. |
| S16 | 4 | B | `s/validation` | Валидация входа и честные ошибки | `cd backend && uv run pytest -q tests/test_validation.py` | проверка пунктов — только regex `^\d+(\.\d+)*\.` по тексту `document.xml`; таймаут тестируется без TestClient, напрямую на `with_timeout`. |
| S17 | 4 | B | `s/e2e` | Сквозной тест по «Как проверяют» | `cd backend && LLM_MODE=mock uv run pytest -q tests/test_e2e_mock.py` | проверка цитаты в п. 3 только по `number` (200 и совпадение номера); в smoke.sh — без проверки `.pdf`. |
| S18 | 5 16:40–17:15 | B | `s/readme` | README | второй человек на своём ноутбуке проходит раздел 8 «Как проверить» с нуля, без ключа, и получает описанный отчёт на тестовом комплекте, не задав ни одного вопроса. | сократи всё, кроме разделов 1–11 официального промпта и раздела 12. |
| S19 | 5 | A | `s/disclosure` | Журнал AI и раскрытие | каждая ветка, принятая мерж-поездом, встречается в журнале ровно один раз, и по строке можно найти коммит: `git log --merges --oneline main` против таблицы. | без иллюстраций — таблица сессий и раздел «Подготовлено до старта». |
| S20 | 5 | B | `s/img` | Скриншоты и схема архитектуры | все ссылки на картинки из README открываются, ни на одной нет секретов. | два скриншота (вкладка «Функции» и панель источника) без схемы. |

Порядок резки при отставании: опциональные рекомендации → dense-поиск (остаётся лексика) → S16 валидация до минимума 422 на формат → UI-полировка → S13 (схемы остаются в модулях). Ядро S04 → S05 → S09 → S10 → S11 → S14 → S08 → S12 не резать.
