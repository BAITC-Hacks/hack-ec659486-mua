# S20: итоговые снимки обязательного сценария

Переснято 23.09.2026 на ветке `s/final-integration`. Таблицы — код `a42d45a`,
заключение — `7b83ed2`; backend и набор данных одинаковы. Интерфейс Docker,
`LLM_MODE=mock`, без ключа и без `NEXT_PUBLIC_USE_FIXTURES`.

Кнопка «Тестовый комплект» создала запуск `16afa83127a5`: `done`, 100%,
`missing_steps=[]`. Использованы редакции 8 и 9 из `data/case11/`.
Результат: 6 изменений подразделений, 209 сопоставлений, 2 подтверждённые потери,
3 кандидата в потери, 12 дублей, 0 конфликтов и 28 ограничений.
Все числа взяты из API; `stats` и хэши документов сохранены в manifest.

| Файл | Что видно |
|---|---|
| `01-upload.png` | Полная страница загрузки с кнопкой «Тестовый комплект» |
| `02-progress.png` | Настоящий промежуточный этап на 80%; затем запуск завершился `done` |
| `03-units.png` | Полная таблица 6 изменений подразделений; длинные списки источников раскрываются |
| `03-units-overview.png` | Верхняя часть отчёта и его счётчики |
| `04-functions.png` | Фильтр подтверждённых потерь: 2 строки |
| `04-functions-new.png` | Фильтр новых функций: 26 строк |
| `05-conflicts.png` | Подтверждённые дубли; в счётчиках 0 конфликтов |
| `06-source.png` | Исходный пункт 3.1 редакции 8, полученный из API |
| `07-conclusion.png` | Сформированное заключение, раскрываемые цитаты, ссылка на Markdown |
| `architecture.png` / `.svg` | Схема S20 по реализации P1–P6, сохранена из исходной ветки |

Снимки получены через CUA с реального интерфейса. DOM, ответы API, текст и числа
не подменялись. Снимки перекодированы в PNG (палитра до 256 цветов) для лимита размера, без ретуши. Визуально проверены: секретов и персональных
путей в кадрах нет; хэши проверенных PNG записаны в `capture-manifest.json`.
Разная высота и ширина кадров обусловлена областью страницы и размером панели браузера.

Проверены клики по вкладкам, фильтры, источники таблицы и заключения (включая п. 3.6.г),
раскрытие цитат. Кнопка скачивания сохранила `orgdiff-16afa83127a5.md`. Файл побайтно совпал с ответом
по адресу кнопки: HTTP 200, `text/markdown`, attachment, 929 632 байта.

Чистый клон отдельно проверен через Docker и локальные `uv`/`npm`: одинаковый `done`.
В Docker проверены 551 уникальный источник и полные цитаты в 542 пунктах.

Проверка целостности, лимита 500 КБ, хэшей и всех ссылок README:

```text
python3 docs/img/check_assets.py
```
