# S03 — ЗЕЛЕНО

Ветка: `s/ui-shell`. Код: `b2d57ee`.

Done-when: `cd frontend && npm run build` — код выхода 0. Next.js 15.5.25 собрал маршруты `/`, `/report/[id]`, `/runs/[id]` без ошибок компиляции и типизации. Проверка запущенного frontend: `/`, `/runs/demo`, `/report/demo` вернули HTTP 200.

Изменённые файлы:
- `frontend/app/layout.tsx` — шапка, навигация и индикатор состояния API/LLM;
- `frontend/app/page.tsx` — заглушка загрузки;
- `frontend/app/runs/[id]/page.tsx` — заглушка прогресса;
- `frontend/app/report/[id]/page.tsx` — четыре вкладки отчёта;
- `frontend/components/status-badge.tsx` — подписи и цвета статусов;
- `frontend/components/ui/tabs.tsx` — компонент вкладок без новых зависимостей.

Загрузка документов, данные отчёта и запросы к API на страницах — задачи последующих сессий S06/S07/S12.

Дополнение после аудита `main`: `StatusBadge` принимает `verification` (`exact`, `lexical`, `llm`) и `verified`. Для непроверенной находки показывает «Требует проверки». Повторный `npm run build` — код выхода 0.
