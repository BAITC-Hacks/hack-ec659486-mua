# S18 — ЗЕЛЕНО (автоматизированная воспроизводимость)

README принят из `s/readme`, включая дополнение `a44be59`. Описание старого partial
заменено результатами итогового набора S15: done, 6 изменений подразделений,
209 сопоставлений, 2 потери, 3 кандидата, 12 дублей, 0 конфликтов.

Проверен чистый клон без пользовательского .env и без ключа:
- Docker: установка и запуск по README, отдельные порты 13000/18000 по разделу 7.5,
  оба сервиса healthy за 171,3 с с кэшем сборки.
- Локально: uv sync --locked, uvicorn, npm ci, npm run dev; тот же отчёт done;
  CORS проверен для адреса локального интерфейса.
- В Docker: 551 уникальный источник, 542 пункта, полные цитаты совпадают;
  экспорт frontend равен backend, Content-Type text/markdown.
- В браузере: тестовый комплект, все вкладки, фильтры потерь и новых функций,
  панель источника, раскрытие цитат заключения; скачанный файл побайтно совпадает
  с API (929 632 байта).
- ESLint, TypeScript, production-сборка — PASS; backend: 206 passed, Ruff — PASS.
- Все относительные ссылки README существуют; проверка PNG и ссылок — PASS.

Независимый ручной запуск вторым участником на его ноутбуке не заявляется.
Live-сценарий через HTTP API целиком не подтверждён: лимит 180 с, запись S15
через служебный pipeline с увеличенным таймаутом заняла 824 с. Ограничение раскрыто.
