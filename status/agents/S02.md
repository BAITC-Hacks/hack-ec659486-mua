# S02 — ЗЕЛЕНО

Ветка `s/fixtures`, worktree `C:\antigravity\github\wt-fixtures`.
Обновление по аудиту `origin/main@b2bc09b` и опубликованному контракту
`origin/s/contract@5138680`: коммит `ba81e2a` запушен в `origin/s/fixtures`.
В `main` изменений и слияний не выполнялось.

## Done-when

Команда из корня репозитория: `cd backend && uv run pytest -q tests/test_fixtures_valid.py`.
Фактически выполнена из `backend`: `uv run pytest -q tests/test_fixtures_valid.py`.

```text
.......                                                                  [100%]
============================== warnings summary ===============================
.venv\Lib\site-packages\starlette\testclient.py:53
  C:\antigravity\github\wt-fixtures\backend\.venv\Lib\site-packages\starlette\testclient.py:53: DeprecationWarning: The anyio.abc.BlockingPortal alias is deprecated, use anyio.from_thread.BlockingPortal instead.
    _PortalFactoryType = Callable[[], AbstractContextManager[anyio.abc.BlockingPortal]]

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
7 passed, 1 warning in 0.62s
```

Предупреждение исходит из установленного Starlette/AnyIO. Зависимости не менялись.
`uv run ruff check tests/test_fixtures_valid.py` → `All checks passed!`.
Строгая проверка TypeScript обеих фикстур с виртуально подставленным
`origin/s/contract:frontend/lib/types.ts` → `Fixture TypeScript validates against published S01 types.ts`.
Чтение контракта выполнялось в памяти, без правки чужих файлов.
`Report.model_validate` и `RunStatus.model_validate` из опубликованного S01 выполняются в тесте
до слияния; после слияния тест возьмёт локальный `app.schemas`.
`git diff --cached --check` → без ошибок.

## Изменённые файлы

- `backend/app/mocks/demo_report.json` — отчёт с новым контрактом, проверками, кандидатами и запретом.
- `frontend/lib/fixtures/report.ts` — тот же объект, тип `Report` из `@/lib/types`.
- `frontend/lib/fixtures/run.ts` — `parsing` 5%, `verification` 65%, `done` 100%; тип `RunStatus` из `@/lib/types`.
- `backend/tests/test_fixtures_valid.py` — семь проверок контракта, источников, связности, кандидатов и статистики.
- `status/agents/S02.md` — этот отчёт.

## Содержимое и ограничения

Отчёт содержит три UnitChange, десять FunctionMatch (по два каждого из пяти требуемых
статусов), один `kind=split` (ДНМ → ДИТААД и ДОА), два кандидата в дубли,
один кандидат в конфликт и один запрет в `constraints` под вводной фразой
«Главный аудитор и работники БВА не имеют права:». `stats.unverified_candidates = 5`:
две потери, два дубля и конфликт. Число подтверждённых потерь, дублей и конфликтов — 0.
`conclusion_md` состоит из десяти строк и не выдаёт эти кандидаты за доказанные выводы.

Примеры взяты из `data/case11/diff-8-vs-9.md`: реорганизация перечня п. 3.4,
кандидат в потерю п. 5.3.6 редакции 8 и добавленное информирование о потенциальном
конфликте при работе с ДЗО в п. 4.4 редакции 9. Кандидат п. 5.3.6 имеет в источниках
п. 5.3.7 редакции 9, где аналогичная обязанность перераспределена: это ещё одна
причина не объявлять функцию потерянной.

TXT в `data/case11` отсутствуют. Цитаты получены из DOCX организаторов через ZIP/XML;
тест повторно извлекает печатные пункты и проверяет каждую цитату по документу,
редакции, `clause_id`, номеру и дословному тексту. Новых файлов в `data/` нет.
Встроенные `before_documents` и `after_documents` содержат цитируемые пункты —
это выборка для интерфейса, не полный разбор документов.

Расхождение исходного промпта «8 FunctionMatch» и «по два kept/changed/lost/new/moved»
разрешено в пользу покрытия пяти статусов: 10 строк. Дубли и конфликт не подтверждены,
поскольку одной похожести формулировок или контроля качества своей команды недостаточно
по аудированным правилам проверки. S02 остаётся вручную подготовленной демонстрационной
фикстурой с подлинными цитатами, а не сохранённым ответом live-модели.
