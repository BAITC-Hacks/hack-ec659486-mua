# S15 — КРАСНО

Ждёт мержа `s/conclusion` (S14) в `origin/main`. Живую запись фикстур не запускал: по условию начала S15 полный код должен содержать `backend/app/conclusion.py` и `backend/app/export_md.py`, а этих файлов в `origin/main` после `git fetch origin` нет. `status/agents/S14.md` и ветка `origin/s/conclusion` также отсутствуют.

## Проверено

- `git fetch origin` — успешно.
- `git merge origin/main` — успешно; модули волн 1–3 теперь доступны в `s/live-fixtures`.
- Проверка обязательных файлов: `llm_schemas.py`, `functions.py`, `candidates.py`, `matching.py`, `duplicates.py`, `rules/conflicts.py` есть; `conclusion.py`, `export_md.py` отсутствуют.

## Не сделано

- `LLM_MODE=live LLM_RECORD_MOCKS=1 ... demo_run.py` — не запускал до мержа S14.
- Повторный demo-run в mock-режиме, полный pytest и запись новых фикстур — после живого прогона.
- Числа отчёта и размер новых фикстур пока не определены.

Следующий шаг: после появления S14 в `origin/main` снова выполнить `git fetch origin && git merge origin/main`, затем живой и mock прогоны по `prompts/sessions/S15-live-fixtures.md`.
