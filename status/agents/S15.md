# S15 — ЗЕЛЕНО

Инструмент: Claude Code (A), сессии `22e8bc92-d7ea-418b-bbc5-6d8825d6479c` (начало записи) и
`a126b339-c078-4b67-a892-55ddea335cd6` (продолжение после обрыва). Ветка `s/live-fixtures`: влиты `origin/main`
и `origin/s/conclusion` (S14), затем один полный живой прогон тестового комплекта с записью ответов модели
`gpt-5.6-luna`. Сессия Codex для S15 запись не начинала: живые вызовы и пуш Codex отклоняет.

## Done-when

- `cd backend && LLM_MODE=mock uv run python ../scripts/ops/demo_run.py` → `итог: done за 3 с`, строк «нет mock-фикстуры» — 0.
- `cd backend && LLM_MODE=mock uv run pytest -q` → `199 passed`.

## Запись

- `LLM_MODE=live LLM_RECORD_MOCKS=1 LLM_TIMEOUT_S=240 LLM_REUSE_RECORDED=1 uv run python ../scripts/ops/demo_run.py`
  → `итог: done за 824 с`.
- Первая попытка в 16:27 упала на шаге «Функции»: `APITimeoutError`, потому что в `app/llm.py` на запрос 40 с. В `scripts/ops/demo_run.py`
  добавлены `LLM_TIMEOUT_S` (таймаут запроса, по умолчанию 240 с, меняется только в этом процессе) и `LLM_REUSE_RECORDED=1`
  (продолжение: ответы, уже записанные в этом worktree, берутся из файлов).
- В 16:42 запись оборвалась вместе с сессией Claude Code на шаге «Проверка» (65%). Продолжена: 102 ответа взяты из записанного
  (extract_units 2, match_units 1, extract_functions 9, verify_matches 89, confirm_loss 1), 214 спрошены у модели
  (verify_matches 66, confirm_loss 4, verify_duplicates 143, write_conclusion 1).
- В mocks/: extract_units 2, match_units 1, extract_functions 9, verify_matches 155, confirm_loss 5, verify_duplicates 143,
  write_conclusion 1, embeddings 357 (12 МБ). Ключа в фикстурах нет: сравнение с `.env` пустое, `OPENAI_API_KEY` упоминается
  только как имя переменной в двух README.

## Итог демо (mock, по живым фикстурам)

Подразделения: 6 (kept 3, transformed 2, created 1). Функции: kept 126, changed 41, moved 11, new 26, lost 2 подтверждено,
lost 3 требует проверки. Дубли: 12, все подтверждены. Конфликты интересов: 0. Ограничения: 28. Заключение сформировано.

## Изменения тестов (владелец — A)

- `tests/test_matching.py`: автофикстура с пустым каталогом векторов. Тесты S10 написаны под mock без векторов, а живые векторы
  комплекта меняли кандидатов (5 падений).
- `tests/test_functions.py`: владелец пункта проверяется по аббревиатуре («БВА»), без скобок. Живой ответ называет подразделение
  «БВА», а не «Блок внутреннего аудита (БВА)» (1 падение).
- `tests/test_units.py`: `test_case11_kit_with_s04_parser` идёт тем же путём, что и демо (живая фикстура), и проверяет инварианты:
  источники, дословные цитаты, должности, контракт S08. Строгая сверка с эталоном `data/case11/diff-8-vs-9.md` остаётся в
  `test_case11_kit` на ручной фикстуре. Живой ответ с эталоном расходится: 6 подразделений вместо 7, created 1 вместо не менее 2
  (ДИТААД и ДОА модель отнесла к преобразованию).

## Не сделано и внимание

- На живых данных правила S11 не нашли ни одного кандидата в конфликт интересов (на ручных фикстурах было 2). Показать на демо
  конфликт нечего; это результат анализа, а не ошибка кода.
- Фикстура `write_conclusion` под `demo_report.json` (тест S14) осталась ручной, её не перезаписывали.
- В `s/live-fixtures` влит `s/conclusion` (S14, уже в `main`), так что вливать можно сразу.
