# S12 — ЗЕЛЕНО

## Done-when

Команда: `cd frontend && npm run build`

Вывод последней попытки (код выхода 0):

```text
> orgdiff-frontend@0.1.0 build
> next build

▲ Next.js 15.5.25
✓ Compiled successfully in 5.1s
Linting and checking validity of types ...
Collecting page data ...
✓ Generating static pages (5/5)
Finalizing page optimization ...
Collecting build traces ...
Route (app)                                 Size  First Load JS
┌ ○ /                                    4.87 kB         107 kB
├ ○ /_not-found                            123 B         103 kB
├ ○ /example                             3.89 kB         110 kB
├ ƒ /report/[id]                         17.8 kB         124 kB
└ ƒ /runs/[id]                           3.46 kB         109 kB
+ First Load JS shared by all             103 kB
```

Первая попытка не запустила Next.js, так как зависимости ещё не были установлены в worktree. После `npm ci` следующая попытка выявила две ошибки ESLint; они исправлены до успешной сборки.

## Изменённые файлы

- `frontend/lib/api-runs.ts` — запрос пункта и переключатель фикстур; отчёт без кэша.
- `frontend/components/run-progress.tsx` — семь шагов и прогресс.
- `frontend/app/runs/[id]/page.tsx` — опрос статуса, переход к отчёту, ошибки.
- `frontend/components/source-drawer.tsx` — полный текст пункта с запасной цитатой.
- `frontend/app/report/[id]/page.tsx` — живой отчёт, ограниченный fallback на фикстуру, панель источника.

## Ограничения

- Сквозной прогон с живым backend не выполнялся: `s/runs` пока снята из merge-train; проверен контракт в `docs/spec-case11.md` и код `origin/s/runs`.
- Промпт S12 упоминает `status.error`, но фактический контракт `RunStatus` в `backend/app/schemas.py` и `frontend/lib/types.ts` передаёт пользовательский текст ошибки в `detail`. Интерфейс показывает `detail` без изменения закрытого типа.
- Других невыполненных пунктов S12 нет.
