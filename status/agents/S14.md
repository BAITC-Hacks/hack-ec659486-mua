# S14 — ЗЕЛЕНО

Ветка: `s/conclusion` (свежий `origin/main` влит fast-forward перед работой).
Код: локальный коммит `fc1a689` — `feat(S14): build sourced conclusions and Markdown export`.

## Done-when

Команда: `cd backend && LLM_MODE=mock uv run pytest -q tests/test_conclusion.py`

Вывод: `6 passed, 1 warning in 0.07s` (предупреждение о deprecated `anyio.abc.BlockingPortal` в `starlette/testclient.py`).

Дополнительно: `uv run ruff check app/conclusion.py app/export_md.py tests/test_conclusion.py` — `All checks passed!`; весь backend: `199 passed, 1 warning in 9.66s`.

## Изменённые файлы

- `backend/app/conclusion.py`
- `backend/app/export_md.py`
- `backend/app/prompts/write_conclusion.md`
- `backend/app/mocks/write_conclusion/1c343000a0e174e3.json`
- `backend/tests/test_conclusion.py`
- `status/agents/S14.md`

## Что не сделано и почему

- Фикстура по `demo_report.json` составлена по находкам вручную; это **не** записанный ответ OpenAI. В окружении есть ключ, но автоматическая проверка дважды отклонила live-вызов, поскольку передача внутренних цитат внешнему сервису сочтена недостаточно подтверждённой. Настоящую фикстуру для демо должен записать S15 при разрешённом live-прогоне.
- `git push -u origin s/conclusion` не выполнен: автоматическая проверка отклонила отправку в GitHub, указав, что назначение `origin` не подтверждено как доверенное. Локальный коммит создан; force-push и merge в `main` не выполнялись.
