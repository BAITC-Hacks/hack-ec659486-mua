# S17 — ЗЕЛЕНО

Ветка: `s/e2e`. Код S17: `61b703d`; затем подтянут `origin/main` с S14.

## Done-when

`cd backend && LLM_MODE=mock uv run pytest -q tests/test_e2e_mock.py --tb=short --show-capture=no`

Вывод: `1 passed, 1 xfailed, 1 warning in 9.03s` (код выхода 0). Xfail предусмотрен уточнением интегратора: API возвращает `partial`, `missing_steps=["conclusion"]`, потому что отсутствует точная фикстура `mocks/write_conclusion/9797d28e3f1f75e5.json` для текущего payload. Проверки содержимого, контрольных случаев, `unverified_candidates`, доступности и цитат всех источников выполняются до xfail и прошли. Экспорт `.md` прошёл отдельным тестом.

## Изменённые файлы

- `backend/tests/test_e2e_mock.py` — реальный demo-run, контрольные находки, проверка источников и Markdown.
- `deploy/smoke.sh` — продуктовый сценарий через API, ожидание результата, проверка отчёта, экспорта и отказа на PDF по контракту S08/S16.
- `status/agents/S17.md` — этот отчёт.

## Что остаётся

S15 должна записать реальные mock-ответы для текущего payload заключения. Сейчас в отчёте 109 кандидатов в дубли, но подтверждённых дублей 0; подтверждённых потерь тоже 0. После S15 повторить Done-when: xfail должен перейти в обычный успешный тест со статусом `done`.

Первый запуск Docker smoke в этом worktree упёрся в занятые порты 8000/3000 от уже работающего официального экземпляра. На отдельных портах тестовый комплект, отчёт и Markdown прошли; PDF вернул HTTP 422. Это правильный код по контракту S08/S16 (`unsupported_format`), поэтому проверка в smoke восстановлена с ожиданием 422 и машинного кода ошибки. Повторный `deploy/smoke.sh` завершился `SMOKE OK`: `unit_changes=7`, `function_matches=222`, `duplicates=109`, frontend HTTP 200; статус запуска `partial` только из-за заключения.
