# S17 — ЗЕЛЕНО

Ветка: `s/e2e`. Код S17: `61b703d`; затем подтянут `origin/main` с S14.

## Done-when

`cd backend && LLM_MODE=mock uv run pytest -q tests/test_e2e_mock.py --tb=short --show-capture=no`

После мержа S15 вывод: `2 passed, 1 warning in 9.30s` (код выхода 0). Полный backend-набор: `206 passed, 1 warning in 43.18s`. Demo в mock-режиме без ключа возвращает `done`, `missing_steps=[]`. Проверки содержания, контрольных случаев, `unverified_candidates`, доступности и цитат источников, а также экспорт `.md` прошли.

## Изменённые файлы

- `backend/tests/test_e2e_mock.py` — реальный demo-run, контрольные находки, проверка источников и Markdown.
- `deploy/smoke.sh` — продуктовый сценарий через API, ожидание результата, проверка отчёта, экспорта и отказа на PDF по контракту S08/S16.
- `status/agents/S17.md` — этот отчёт.

## Что остаётся

S15 записала реальные mock-ответы для текущего payload заключения. Контейнерный smoke после их мержа завершился `SMOKE OK`: demo `done`, `missing_steps=[]`, 5 изменений подразделений, 187 строк сопоставления функций, 11 дублей, frontend HTTP 200.

Первый запуск Docker smoke в этом worktree упёрся в занятые порты 8000/3000 от уже работающего официального экземпляра. На отдельных портах проверка PDF подтвердила HTTP 422 и `unsupported_format` по контракту S08/S16. После S15 повторный `deploy/smoke.sh` завершился `SMOKE OK` со статусом `done`.
